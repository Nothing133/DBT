version: 2

models:
  - name: TestUnique
    columns:
      - name: unique_value
        tests:
          - unique
          - not_null
          - accepted_values:
              values: ["value1", "value2"]
